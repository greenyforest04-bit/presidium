//! LLM-specific entities — models, inference sessions.

// Stub: LLM entities will be defined as the inference layer evolves.
// Planned entities:
// - LlmModel (loaded GGUF model)
// - InferenceSession (active inference context)
// - EmbeddingVector (semantic embedding)
